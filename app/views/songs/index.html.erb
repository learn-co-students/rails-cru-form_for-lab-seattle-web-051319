<h1>Songs</h1>
<div>
<% @songs.each do |song| %>
  <div><p><%=link_to song.name, song_path(song)%></p></div>
  <div>Artist: <%=link_to song.artist.name, artist_path(song.artist)%></div>
  <div>Genre: <%=link_to song.genre.name, genre_path(song.genre)%></div><br>
<% end %>
</div>
